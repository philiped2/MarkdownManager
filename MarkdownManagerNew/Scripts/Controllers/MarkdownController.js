(function () {
    var app = angular.module("app");

    var MarkdownController = function ($scope, $http, $log) {
        var mdText = $scope.mdText;
    }
    app.controller("MarkdownController", MarkdownController);
}());